<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyectofinalmoviles">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_fruits_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/marketplace_amico"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalMoviles"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SupermarketActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".Credits"
            android:exported="true">
        </activity>

        <activity
            android:name=".FruitsActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".AdminActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".DairyActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".VegetablesActivity"
            android:exported="true">
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>